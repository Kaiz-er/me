export default function ProjectCardPill({ detail }) {
  return (
    <div className="flex justify-center items-center rounded-full bg-gray-200 text-xs text-neutral-700">
      {detail}
    </div>
  );
}
